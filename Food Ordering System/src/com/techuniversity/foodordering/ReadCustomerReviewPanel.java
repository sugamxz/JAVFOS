package com.techuniversity.foodordering;

import javax.swing.*;
import java.awt.*;

public class ReadCustomerReviewPanel extends JPanel {
    public ReadCustomerReviewPanel() {
        setLayout(new BorderLayout());

        // Placeholder for Read Customer Review Panel
        JLabel placeholderLabel = new JLabel("Read Customer Review Panel");
        placeholderLabel.setFont(new Font("Arial", Font.PLAIN, 18));
        placeholderLabel.setHorizontalAlignment(SwingConstants.CENTER);
        add(placeholderLabel, BorderLayout.CENTER);

        // You can add more components here for customer review functionality
    }
}
