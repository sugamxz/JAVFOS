package com.techuniversity.foodordering;

import javax.swing.*;
import java.awt.*;

public class CheckTransactionHistoryPanel extends JPanel {
    public CheckTransactionHistoryPanel() {
        setLayout(new BorderLayout());

        // Placeholder for Check Transaction History Panel
        JLabel placeholderLabel = new JLabel("Check Transaction History Panel");
        placeholderLabel.setFont(new Font("Arial", Font.PLAIN, 18));
        placeholderLabel.setHorizontalAlignment(SwingConstants.CENTER);
        add(placeholderLabel, BorderLayout.CENTER);

        // You can add more components here for checking transaction history functionality
    }
}
